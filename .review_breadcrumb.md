## Review: issue #469

### Summary

Custom workflow templates in policy config. Adds `workflow_templates: Vec<WorkflowTemplate>` to both `RunnerConfig` and `RepoPolicy`, with custom templates taking precedence over built-ins.

### Issues found

| Severity | File | Line | Description |
|----------|------|------|-------------|
| medium | `workflow.rs` | 114 | Title-inferred fallback path ignores `policy.workflow_templates` — only `builtin_templates()` is searched. Label-based path correctly checks custom templates first; title-inferred path does not. If a user defines a custom `"bug"` template in `RepoPolicy.workflow_templates`, it will be used for issues labelled `"bug"` but NOT for issues whose title starts with `"fix"`/`"bug"`. Inconsistency, but not a panic or data loss. |

### Correctness

- `RunnerConfig.resolve_workflow()` correctly checks custom templates before built-ins — used in both orchestrator and dry-run path. Primary route-based code path is correct.
- `RepoPolicy.workflow_templates` is wired into `select_workflow()` for label-based routing — correct.
- `#[serde(default)]` on both fields means existing configs deserialize without change.
- All 40 lib tests pass.

### Tests

- `config::tests::parse_workflow_templates_toml` — TOML round-trip ✓
- `config::tests::custom_workflow_template_overrides_builtin` — override semantics ✓
- `config::tests::resolve_workflow_falls_back_to_builtin_when_no_custom` — fallback ✓
- `workflow::tests::select_workflow_custom_template_via_label_mapping` — label path ✓
- `workflow::tests::select_workflow_custom_overrides_builtin_with_same_name` — precedence ✓
- `workflow::tests::select_workflow_falls_back_to_builtin_when_no_custom` — fallback ✓
- No test for title-inferred path with custom templates (gap matching the code gap above).

### Code quality

- No panics, no unsafe, no unwrap on user-controlled input.
- Style is consistent with surrounding code.
- `WorkflowTemplate` derives `Clone` as needed; no `PartialEq` needed (tests compare fields directly).

### Verdict: PASS

No high-severity issues. One medium-severity inconsistency (title-inferred routing in `select_workflow` does not check custom templates), but the primary route-based path (`RunnerConfig` + `resolve_workflow`) is correct and fully tested.
