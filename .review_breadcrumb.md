## Review: issue #494

### Summary

Removes title-prefix heuristic from `select_workflow` and replaces it with label-based lookup + configurable `default_workflow`. Two new fields added to `RepoPolicy`: `default_workflow: Option<String>` and `workflow_templates: Vec<WorkflowTemplate>`.

Files changed:
- `crates/claude-runner/src/workflow.rs` — core logic change + new tests
- `crates/claude-runner/src/policy.rs` — new struct fields
- `crates/claude-runner/src/planner.rs` — test helper updated for new fields

### Issues found

| Severity | File | Line | Description |
|----------|------|------|-------------|
| low | workflow.rs | 104-109 | `unwrap_or_else` rebuilds `builtin_templates()` a second time to find `"feature"`, when `all` already contains it. Only fires when `default_workflow` names a non-existent template. Minor inefficiency, not a bug; "feature" is a guaranteed built-in so the `unwrap()` is safe. |
| low | workflow.rs | 104-109 | If `default_workflow` is set to an unknown name, the selection silently falls back to `"feature"` with no warning. Could surprise users who mistype a template name. Intentional for robustness; acceptable for now. |

### Checklist

- [x] Title-prefix inference block fully removed
- [x] Label-to-template mapping resolves against custom + built-in templates (custom shadows built-in)
- [x] `default_workflow` configurable fallback works correctly
- [x] Ultimate fallback to `"feature"` is safe (built-in is guaranteed)
- [x] `#[serde(default)]` on new fields — existing policy files won't break
- [x] Tests cover: label match, configured default, no-default fallback, title-prefix regression, custom template shadowing
- [x] All three `select_workflow` call sites in `orchestrator.rs` are unaffected (signature unchanged)
- [x] Style and doc-comment conventions match surrounding code

### Verdict: PASS
